/* heaps -> a heap is a complete binary tree that follows the heap order property 
- complete Binary tree (cbt) -> all levels are completely filled except the last  level, the last level is fileed from left to right 
- heap order property -> 1. Max heap -> every parent node is greter than or equal to its children the largest element is always at root
                           2. Min heap -> every parent node is smaller than or equal to its children the smallest


for any node 
. left child
. right child 
parent i/2
